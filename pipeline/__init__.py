"""Topic-wise PYQ study-material pipeline.

Every module here uses repo-relative paths (see paths.py) so the pipeline runs
unchanged on any machine — unlike the older top-level scripts, several of which
hardcode a personal macOS home directory.
"""
